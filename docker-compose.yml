services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-jobpilot}
      POSTGRES_USER: ${POSTGRES_USER:-jobpilot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-jobpilot}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-jobpilot} -d ${POSTGRES_DB:-jobpilot}"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - jobpilot_db:/var/lib/postgresql/data

  api:
    build:
      context: ./api
    entrypoint: ["sh", "./docker-entrypoint.sh"]
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      APP_SECRET: ${APP_SECRET:-change-me}
      APP_ENCRYPTION_KEY: ${APP_ENCRYPTION_KEY:-}
      DATABASE_URL: postgresql://${POSTGRES_USER:-jobpilot}:${POSTGRES_PASSWORD:-jobpilot}@db:5432/${POSTGRES_DB:-jobpilot}?serverVersion=17&charset=utf8
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:8080/api/integrations/gmail/callback}
      WEB_URL: ${WEB_URL:-http://localhost:3000}
      GMAIL_SEARCH_QUERY: "${GMAIL_SEARCH_QUERY:-(job OR mission OR candidature OR application OR recruiter OR entretien) newer_than:30d}"
      GMAIL_MAX_RESULTS: ${GMAIL_MAX_RESULTS:-100}
      GMAIL_MAX_PAGES: ${GMAIL_MAX_PAGES:-3}
      ADZUNA_APP_ID: ${ADZUNA_APP_ID:-}
      ADZUNA_APP_KEY: ${ADZUNA_APP_KEY:-}
      ADZUNA_COUNTRY: ${ADZUNA_COUNTRY:-fr}
      ADZUNA_WHERE: ${ADZUNA_WHERE:-}
      ADZUNA_RESULTS_PER_QUERY: ${ADZUNA_RESULTS_PER_QUERY:-20}
      ARBEITNOW_ENABLED: ${ARBEITNOW_ENABLED:-1}
      ARBEITNOW_PAGES: ${ARBEITNOW_PAGES:-3}
      JOB_SYNC_INTERVAL_SECONDS: ${JOB_SYNC_INTERVAL_SECONDS:-21600}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "127.0.0.1:8080:8080"
    healthcheck:
      test: ["CMD", "php", "-r", "exit(@file_get_contents('http://127.0.0.1:8080/api/health') === false ? 1 : 0);"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s
    volumes:
      - ./initial-data:/app/initial-data:ro
      - jobpilot_private:/app/var/private

  scheduler:
    build:
      context: ./api
    entrypoint:
      - sh
      - -c
      - |
        while true; do
          php bin/console app:jobs:sync || true
          php bin/console app:applications:auto-submit || true
          sleep "$${JOB_SYNC_INTERVAL_SECONDS:-21600}"
        done
    restart: unless-stopped
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      APP_SECRET: ${APP_SECRET:-change-me}
      APP_ENCRYPTION_KEY: ${APP_ENCRYPTION_KEY:-}
      DATABASE_URL: postgresql://${POSTGRES_USER:-jobpilot}:${POSTGRES_PASSWORD:-jobpilot}@db:5432/${POSTGRES_DB:-jobpilot}?serverVersion=17&charset=utf8
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:8080/api/integrations/gmail/callback}
      WEB_URL: ${WEB_URL:-http://localhost:3000}
      GMAIL_SEARCH_QUERY: "${GMAIL_SEARCH_QUERY:-(job OR mission OR candidature OR application OR recruiter OR entretien) newer_than:30d}"
      GMAIL_MAX_RESULTS: ${GMAIL_MAX_RESULTS:-100}
      GMAIL_MAX_PAGES: ${GMAIL_MAX_PAGES:-3}
      ADZUNA_APP_ID: ${ADZUNA_APP_ID:-}
      ADZUNA_APP_KEY: ${ADZUNA_APP_KEY:-}
      ADZUNA_COUNTRY: ${ADZUNA_COUNTRY:-fr}
      ADZUNA_WHERE: ${ADZUNA_WHERE:-}
      ADZUNA_RESULTS_PER_QUERY: ${ADZUNA_RESULTS_PER_QUERY:-20}
      ARBEITNOW_ENABLED: ${ARBEITNOW_ENABLED:-1}
      ARBEITNOW_PAGES: ${ARBEITNOW_PAGES:-3}
      JOB_SYNC_INTERVAL_SECONDS: ${JOB_SYNC_INTERVAL_SECONDS:-21600}
    depends_on:
      api:
        condition: service_healthy
    volumes:
      - ./initial-data:/app/initial-data:ro
      - jobpilot_private:/app/var/private

  web:
    build:
      context: ./web
    environment:
      NEXT_PUBLIC_API_URL: /api
      API_INTERNAL_URL: http://api:8080
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "127.0.0.1:3000:3000"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:3000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s

volumes:
  jobpilot_db:
  jobpilot_private:
